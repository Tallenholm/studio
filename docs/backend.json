{
  "entities": {
    "Vehicle": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Vehicle",
      "type": "object",
      "description": "Represents a vehicle in the fleet, such as a truck, trailer, or skid steer.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the vehicle."
        },
        "vin": {
          "type": "string",
          "description": "Vehicle Identification Number (VIN) of the vehicle."
        },
        "year": {
          "type": "number",
          "description": "The manufacturing year of the vehicle."
        },
        "make": {
          "type": "string",
          "description": "The make of the vehicle (e.g., Chevy)."
        },
        "model": {
          "type": "string",
          "description": "The model of the vehicle (e.g., 6500)."
        },
        "type": {
          "type": "string",
          "description": "The type of vehicle (e.g., truck, trailer, skid steer)."
        },
        "bedType": {
          "type": "string",
          "description": "The bed type of the vehicle (e.g., dump bed, tilt deck)."
        }
      },
      "required": [
        "id",
        "vin",
        "year",
        "make",
        "model",
        "type"
      ]
    },
    "ChecklistItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "ChecklistItem",
      "type": "object",
      "description": "Represents a single item on a pre-trip or post-trip inspection checklist.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the checklist item."
        },
        "description": {
          "type": "string",
          "description": "Description of the inspection item."
        },
        "instructions": {
          "type": "string",
          "description": "Detailed instructions for performing the inspection."
        },
        "vehicleType": {
          "type": "string",
          "description": "The Vehicle Type the inspection applies to."
        }
      },
      "required": [
        "id",
        "description",
        "instructions"
      ]
    },
    "InspectionReport": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InspectionReport",
      "type": "object",
      "description": "Represents a pre-trip or post-trip inspection report for a vehicle.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the inspection report."
        },
        "vehicleId": {
          "type": "string",
          "description": "Reference to Vehicle. (Relationship: Vehicle 1:N InspectionReport)"
        },
        "date": {
          "type": "string",
          "description": "Date and time the inspection was performed.",
          "format": "date-time"
        },
        "type": {
          "type": "string",
          "description": "The type of inspection (pre-trip or post-trip)."
        },
        "notes": {
          "type": "string",
          "description": "Additional notes or comments about the inspection."
        }
      },
      "required": [
        "id",
        "vehicleId",
        "date",
        "type"
      ]
    },
    "InspectionResult": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InspectionResult",
      "type": "object",
      "description": "Represents the result of a single checklist item in an inspection report.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the inspection result."
        },
        "reportId": {
          "type": "string",
          "description": "Reference to InspectionReport. (Relationship: InspectionReport 1:N InspectionResult)"
        },
        "checklistItemId": {
          "type": "string",
          "description": "Reference to ChecklistItem. (Relationship: ChecklistItem 1:N InspectionResult)"
        },
        "status": {
          "type": "string",
          "description": "The status of the checklist item (pass or fail)."
        }
      },
      "required": [
        "id",
        "reportId",
        "checklistItemId",
        "status"
      ]
    },
    "InventoryItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "InventoryItem",
      "type": "object",
      "description": "Represents a single item in the company's inventory, such as a tool or material.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the inventory item." },
        "name": { "type": "string", "description": "The name of the item." },
        "type": { "type": "string", "enum": ["tool", "material", "consumable"], "description": "The type of inventory item." },
        "category": { "type": "string", "description": "A user-defined category for grouping items (e.g., 'Power Tools')." },
        "quantity": { "type": "number", "description": "The quantity of the item available." },
        "status": { "type": "string", "enum": ["available", "in_use", "maintenance", "lost"], "description": "The current status of the item." },
        "assignedToType": { "type": "string", "enum": ["employee", "job", "vehicle"], "description": "The type of entity the item is assigned to, if in use." },
        "assignedToId": { "type": "string", "description": "The ID of the entity the item is assigned to." },
        "assignedToName": { "type": "string", "description": "The denormalized name of the entity for easy display." }
      },
      "required": ["id", "name", "type", "quantity", "status"]
    },
    "Rental": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Rental",
      "type": "object",
      "description": "Represents an equipment rental agreement with a third party.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the rental." },
        "assetId": { "type": "string", "description": "The ID of the fleet asset being rented." },
        "assetName": { "type": "string", "description": "Denormalized name of the asset for easy display." },
        "renterName": { "type": "string", "description": "The name of the person or company renting the asset." },
        "contactInfo": { "type": "string", "description": "Contact information for the renter." },
        "startDate": { "type": "string", "format": "date", "description": "The start date of the rental period." },
        "endDate": { "type": "string", "format": "date", "description": "The end date of the rental period." },
        "rate": { "type": "number", "description": "The rental rate." },
        "rateType": { "type": "string", "enum": ["daily", "weekly", "monthly"], "description": "The type of rate (e.g., daily, weekly)." },
        "notes": { "type": "string", "description": "Additional notes about the rental." }
      },
      "required": ["id", "assetId", "assetName", "renterName", "startDate", "endDate", "rate", "rateType"]
    },
    "Violation": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Violation",
      "type": "object",
      "description": "Represents a documented employee violation for safety, conduct, or performance.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the violation record." },
        "employeeId": { "type": "string", "description": "The ID of the employee who committed the violation." },
        "employeeName": { "type": "string", "description": "Denormalized name of the employee for easy display." },
        "date": { "type": "string", "format": "date", "description": "The date the violation occurred." },
        "type": { "type": "string", "enum": ["safety", "conduct", "performance", "other"], "description": "The category of the violation." },
        "description": { "type": "string", "description": "A detailed, objective description of the incident." },
        "actionTaken": { "type": "string", "description": "The disciplinary or corrective action that was taken." }
      },
      "required": ["id", "employeeId", "employeeName", "date", "type", "description", "actionTaken"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "google.com"
    ]
  },
  "firestore": {
    "/vehicles/{vehicleId}": {
      "schema": {
        "$ref": "#/backend/entities/Vehicle"
      },
      "description": "Stores all information about a single vehicle, including its specifications and maintenance schedule. This collection is the source of truth for all fleet assets."
    },
    "/checklist_items/{checklistItemId}": {
      "schema": {
        "$ref": "#/backend/entities/ChecklistItem"
      },
      "description": "This is a static collection containing the master list of all possible inspection items for all vehicle types. It is used to generate the checklists dynamically on the client."
    },
    "/inspection_reports/{inspectionReportId}": {
      "schema": {
        "$ref": "#/backend/entities/InspectionReport"
      },
      "description": "Contains the main document for each inspection report. It links to the vehicles and user who performed the inspection and holds the overall status."
    },
    "/inspection_reports/{inspectionReportId}/inspection_results/{inspectionResultId}": {
      "schema": {
        "$ref": "#/backend/entities/InspectionResult"
      },
      "description": "A subcollection storing the pass/fail result for each individual item within a specific inspection report. This allows for detailed reporting and analysis."
    },
    "/inventory/{inventoryItemId}": {
      "schema": {
        "$ref": "#/backend/entities/InventoryItem"
      },
      "description": "Tracks all company inventory, including tools, materials, and consumables."
    },
    "/rentals/{rentalId}": {
      "schema": {
        "$ref": "#/backend/entities/Rental"
      },
      "description": "Tracks all third-party equipment rentals."
    },
    "/violations/{violationId}": {
      "schema": {
        "$ref": "#/backend/entities/Violation"
      },
      "description": "Tracks all documented employee violations for internal records."
    }
  }
}
